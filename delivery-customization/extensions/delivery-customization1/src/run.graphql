query RunInput {
  cart {
    deliveryGroups {
      deliveryAddress {
        provinceCode
        zip
      }
      deliveryOptions {
        handle
        title
      }
    }
    attribute(key: "user_pincode") {
      key
      value
    }
  }
}
