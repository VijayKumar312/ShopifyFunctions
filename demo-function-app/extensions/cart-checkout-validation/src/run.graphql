query RunInput {
  cart {
    lines {
      quantity
    }
    buyerIdentity {
      customer {
        numberOfOrders
      }
    }
    cost {
      subtotalAmount {
        amount
      }
    }
  }
}
